export const metadata = {
  title: "Nwalhost AI",
  description: "Asisten AI buatan Nwalhost",
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  );
}
